/*
  # REMOVE THIS LINE AT LAUNCH: keeps the client-review deployment out of
  # search results so it cannot compete with the live bartsbarbershop.org site.
  X-Robots-Tag: noindex, nofollow
  # Baseline security headers (keep these after launch).
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
